name: "cache action runtime"

inputs:
  auth-token:
    required: true
  github-token:
    required: false
  action-ref:
    required: false
  user:
    required: true
  cache:
    required: true
  upstream-caches:
    required: false

runs:
  using: "node24"
  main: "main.js"
  post: "post.js"
